## What

<!-- What does this change do? -->

## Why

<!-- The problem it solves. Link the issue: Fixes #123 -->

## Checklist

- [ ] `dotnet build Ssabba.slnx` succeeds
- [ ] `dotnet test Ssabba.slnx` passes
- [ ] `dotnet format Ssabba.slnx` leaves no changes (CI runs `--verify-no-changes`)
- [ ] A migration was added if the model changed
- [ ] Documentation under `docs/` or the README was updated if behaviour changed
