server {
    listen 8080;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location /docs/ {
        try_files $uri $uri/ $uri/index.html =404;
    }

    location = /docs {
        return 301 /docs/;
    }
}
